#include<bits/stdc++.h>
using namespace std;

#define endl '\n'
const long long MOD = 1e9 + 7;
const long long INF = LLONG_MAX >> 1;  // 4 * 10^18 ( INF + INF will not give Error )

// IN CP, most of the Problems, you will not allow to cross 1 * 10^18, therefore our INF more likely unreachable

int main() {

    ios::sync_with_stdio(false); cin.tie(NULL);

    int t; cin >> t;

    while(t--){
        
    }

    return 0;

}